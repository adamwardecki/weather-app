var Syncano = require('./src/syncano.js');

var connection = new Syncano({ accountKey: 'fd586cff7e631a01158ceeeeee8c8fc460d7cba0', monitorConnection: true, baseUrl: 'https://api.syncano.rocks'});

connection.monitor.onConnect(function() {
  console.log(connection.monitor.isConnected());
});

connection.monitor.onDisconnect(function() {
  console.log(connection.monitor.isConnected());
});
